<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.rhapp.rhapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RHAList"
            android:label="@string/title_activity_rhalist" >
        </activity>
        <activity
            android:name=".SendMessageToAll"
            android:label="@string/title_activity_send_message_to_all" >
        </activity>
        <activity
            android:name=".SendPersonalMessage"
            android:label="@string/title_activity_send_personal_message" >
        </activity>
    </application>

</manifest>
